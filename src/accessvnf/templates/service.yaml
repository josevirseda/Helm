apiVersion: v1
kind: Service
metadata:
  name: {{ include "accessvnf.fullname" . }}
  labels:
    {{- include "accessvnf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accessvnf.selectorLabels" . | nindent 4 }}
