apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accessvnf.fullname" . }}-test-connection"
  labels:
    {{- include "accessvnf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "accessvnf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
