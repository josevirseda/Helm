apiVersion: v1
kind: Service
metadata:
  name: {{ include "ext.fullname" . }}
  labels:
    {{- include "ext.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ext.selectorLabels" . | nindent 4 }}
