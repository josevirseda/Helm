apiVersion: v1
kind: Service
metadata:
  name: {{ include "AccessNet1.fullname" . }}
  labels:
    {{- include "AccessNet1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "AccessNet1.selectorLabels" . | nindent 4 }}
