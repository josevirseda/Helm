apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ejemploubuntuextnet.fullname" . }}-test-connection"
  labels:
    {{- include "ejemploubuntuextnet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ejemploubuntuextnet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
