apiVersion: v1
kind: Service
metadata:
  name: {{ include "extnetvnf.fullname" . }}
  labels:
    {{- include "extnetvnf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "extnetvnf.selectorLabels" . | nindent 4 }}
