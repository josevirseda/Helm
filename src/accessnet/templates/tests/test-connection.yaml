apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accessnet.fullname" . }}-test-connection"
  labels:
    {{- include "accessnet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "accessnet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
