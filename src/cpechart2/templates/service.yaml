apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpechart2.fullname" . }}
  labels:
    {{- include "cpechart2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpechart2.selectorLabels" . | nindent 4 }}
