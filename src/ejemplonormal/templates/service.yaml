apiVersion: v1
kind: Service
metadata:
  name: {{ include "ejemplonormal.fullname" . }}
  labels:
    {{- include "ejemplonormal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ejemplonormal.selectorLabels" . | nindent 4 }}
