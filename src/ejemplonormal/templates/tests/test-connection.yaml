apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ejemplonormal.fullname" . }}-test-connection"
  labels:
    {{- include "ejemplonormal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ejemplonormal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
