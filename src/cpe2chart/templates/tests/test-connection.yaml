apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpe2chart.fullname" . }}-test-connection"
  labels:
    {{- include "cpe2chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpe2chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
