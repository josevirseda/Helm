apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpe2chart.fullname" . }}
  labels:
    {{- include "cpe2chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpe2chart.selectorLabels" . | nindent 4 }}
