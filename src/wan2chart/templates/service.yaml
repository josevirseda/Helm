apiVersion: v1
kind: Service
metadata:
  name: {{ include "wan2chart.fullname" . }}
  labels:
    {{- include "wan2chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wan2chart.selectorLabels" . | nindent 4 }}
