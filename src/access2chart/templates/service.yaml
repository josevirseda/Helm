apiVersion: v1
kind: Service
metadata:
  name: {{ include "access2chart.fullname" . }}
  labels:
    {{- include "access2chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "access2chart.selectorLabels" . | nindent 4 }}
