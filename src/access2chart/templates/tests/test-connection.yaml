apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "access2chart.fullname" . }}-test-connection"
  labels:
    {{- include "access2chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "access2chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
